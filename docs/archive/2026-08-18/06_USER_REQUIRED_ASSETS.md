# 阶段2待补资源与用户决定

## 当前状态

验收点A已通过，验收B的功能前置已接入。八区域、三种工具、八个袋子Accessory、五种叶子、二十五种失物模板和基础HUD均已存在，没有缺失的必制模型阻塞内部3次跑图。

本文件只记录需要用户提供或最终确认的外部资源与产品参数。

## 需要补齐的图片与文字

- `BagConfig.Levels[3～8].Image`目前为空，需要每级袋子HUD图片AssetId。
- 自定义快捷栏已经读取Tool01/02/03的`ToolConfig.Image`，但三项目前均为空；需要在`src/shared/Config/ToolConfig.luau`分别填写正式AssetId。为空时快捷栏只显示工具名称，不显示`ImageLabel`。
- `LostItemConfig`的25项仍使用同一占位图片和各稀有度通用介绍，需要逐项填写正式图片、名称和介绍。
- `ReplicatedStorage.LostItem.LostItem01～25.Model`已存在，无需重新创建；需验收其相对`upgrade`的位置、Tool握持角度和图鉴图片是否一一对应。
- `ReplicatedStorage.LostItem.LostItem100`固定作为教程手机接入；需验收其Area_04～08世界落地、12次揭晓、无需清叶直接拾取和Tool握持效果，不需要图鉴图片。
- `ReplicatedStorage.LostItem.LostItem01.upgrade.BillboardGui`是全部失物共用的价值牌模板；`Workspace.LostItem`只用于场景装扮，不需要为玩法同步或保留其副本。
- `ReplicatedStorage.Key.Key02～Key08`是已停用的保留素材，不需要补图、调整或验收，也不影响运行时。

图片填写格式为`rbxassetid://数字`。资源尚未提供时保持空字符串或现有占位图，不创建虚假AssetId。

## 已进入实测的经济参数

- 袋子容量为`50/85/130/200/300/450/650/900`，当前验收价格为`0/45/150/400/1000/2500/6000/15000`。
- Tool02扫把和Tool03吹风机当前解锁价格分别为500和3000。
- 双手、扫把和吹风机各升级分支已有价格表，是验收B第一轮参数，尚未被真实玩家数据证明平衡。
- 下水道系统已移除，不再需要井盖模型、价格属性或Prompt素材。
- 调整时应保留每轮配置版本和原始记录，不再把0价袋子或1金币工具当作有效规格。

## 需要进行的视觉与实机验收

- Tool01/02/03在固定第三人称下的握持方向、动作动画与镜头遮挡。
- 自定义快捷栏在桌面和移动端的按钮尺寸、工具名称可读性、选中描边，以及最多3个清洁工具加1件失物时的横向布局。
- 扫把聚拢和吹风机推动叶子的速度、扩散、停止位置与3000片叶子时的网络观感。
- `bag01～bag08`在不同Avatar体型上的位置、缩放和穿模。
- `LostItem01～25`世界落地、Tool握持、`upgrade`隐藏和图鉴图像对应关系。
- `LostItem100`手机的个人生成、拾取携带和1000基础金币兑换表现。
- Area_02～08回收已接入，需要验证各区Prompt、回收动画和金币反馈；预放置商品仍未接入。
- 清理时间排行榜现直接复用`Workspace.Function.Leaderboards.WinsBoard`；需要在正确Place验收Top 20行模板的间距、ScrollingFrame滚动范围和不同客户端各自的Self行，不需要新制作排行榜资产。

## DataStore验收条件

- Studio中开启“允许Studio访问API服务”后，验证金币、25种图鉴次数及已领取档位重进保存。
- 用SchemaVersion 1且含01～10旧计数的测试档验证旧值不变、11～25从0开始；用含钻石和`KeyCounts`的Schema 11测试档验证钻石只按1:10转换一次、钥匙字段被忽略且其他数据不受影响。
- 金币、袋子、工具、升级、区域进度和本轮个人失物计划正常存档；失物世界坐标与携带Tool不保存，已触发未兑换物重进后会重新选择地面恢复。完成庄园后的主动重生按重生边界清空本轮计划和养成。

## 不需要现在制作

- 多人大厅、Teleport UI、队伍面板和多人地图资产。
- Tool05已使用`ReplicatedStorage.CleaningTools.Tool05`正式模型；Tool06或更多工具仍需先完成对应玩法设计并提供模型。
- 最终假牙章节、复杂剧情、商业化商店或其他排行榜资产。

只有外部资源确实阻塞验收B时，才在本文件新增条目，并写明准确路径、Instance类型、命名、枢轴/尺寸要求和验收方式。
